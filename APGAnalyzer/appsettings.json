{
  // ConnectionStrings:DefaultConnection is intentionally NOT set here.
  // - In Development:  loaded from appsettings.Development.json (LocalDB).
  // - In Production:   loaded from Azure App Service > Configuration > Connection strings
  //                    (the Visual Studio Publish wizard creates this for you when
  //                    it provisions the Azure SQL Database).
  // - On-prem deploys: loaded from appsettings.Production.json or environment vars.

  // Application Insights telemetry (the app's "security camera").
  // In Production, Azure App Service automatically injects APPLICATIONINSIGHTS_CONNECTION_STRING
  // as an environment variable when App Insights is enabled via the portal — that value
  // takes precedence over this one. This entry is used for local development only.
  "ApplicationInsights": {
    "ConnectionString": "InstrumentationKey=8c623564-2789-44ea-8003-1000707b35fe;IngestionEndpoint=https://centralus-2.in.applicationinsights.azure.com/;LiveEndpoint=https://centralus.livediagnostics.monitor.azure.com/;ApplicationId=496310ea-72eb-495d-ae7e-f8598dd38b89"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
